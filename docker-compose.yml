services:
  backend:
    build: .
    container_name: fraudia_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # Sincronización en vivo de tu código
    env_file:
      - .env
    command: uvicorn src.app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir /app/src